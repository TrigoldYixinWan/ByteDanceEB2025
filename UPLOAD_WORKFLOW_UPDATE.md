# 📤 文档上传工作流程更新

**更新日期**: 2025-12-11  
**状态**: ✅ 已实施

---

## 🎯 更新目标

将文档上传流程分为两个独立阶段：
1. **上传阶段**：文件上传到 Storage + 创建数据库记录（status='pending'）
2. **处理阶段**（Phase 4）：Chunk 分割 + 向量化（status='processing' → 'ready'）

---

## 📊 新的工作流程

### 阶段 1: 文档上传（✅ 已实现）

```
用户操作流程：
┌─────────────────────────────────────────────────┐
│ 1. 访问 /admin/upload                           │
│                                                 │
│ 2. 选择文件（拖放或点击）                       │
│    ↓                                            │
│    显示文件预览（名称、大小、类型）              │
│                                                 │
│ 3. 填写表单信息                                 │
│    - 标题 *                                     │
│    - 类别 * (8个选项)                           │
│    - 子类别（可选）                             │
│                                                 │
│ 4. 点击"上传文档"按钮                           │
│    ↓                                            │
│    显示上传进度条 (0% → 100%)                   │
│    ↓                                            │
│    显示成功消息 ✅                              │
│    ↓                                            │
│    3秒后自动跳转到 Dashboard                    │
└─────────────────────────────────────────────────┘

后端处理流程：
┌─────────────────────────────────────────────────┐
│ POST /api/documents                             │
│                                                 │
│ 1. 验证文件（类型、大小）                       │
│ 2. 上传到 Supabase Storage                     │
│    路径: {userId}/{timestamp}-{filename}        │
│ 3. 获取公开 URL                                 │
│ 4. 创建数据库记录                               │
│    - status: 'pending' ✨（待处理）            │
│ 5. 返回成功响应                                 │
└─────────────────────────────────────────────────┘
```

### 阶段 2: 文档处理（⏳ Phase 4 待实现）

```
处理触发方式（二选一）：

方案 A: 手动触发
┌─────────────────────────────────────────────────┐
│ 管理员在 Dashboard 看到状态为"待处理"的文档     │
│ ↓                                               │
│ 点击"开始处理"按钮                              │
│ ↓                                               │
│ 后台任务开始处理                                │
│   - status: 'pending' → 'processing'           │
│   - PDF 解析                                    │
│   - 文本切分（Chunking）                        │
│   - 向量化（Embeddings）                        │
│   - 插入 document_chunks 表                     │
│ ↓                                               │
│ 处理完成                                        │
│   - status: 'processing' → 'ready'             │
└─────────────────────────────────────────────────┘

方案 B: 自动后台任务
┌─────────────────────────────────────────────────┐
│ 后台定时任务（每分钟）                          │
│ ↓                                               │
│ 查询 status='pending' 的文档                    │
│ ↓                                               │
│ 逐个处理（同上）                                │
└─────────────────────────────────────────────────┘
```

---

## 🎨 UI/UX 改进

### 1. 文件预览卡片

**选择文件后立即显示**:

```
┌─────────────────────────────────────────────────┐
│  📄  merchant-guide.pdf                         │
│     2.3 MB • application/pdf                    │
│                                          [🗑️]  │
│                                                 │
│  上传中... ▰▰▰▰▰▰▰▱▱▱ 75%                      │
└─────────────────────────────────────────────────┘
```

**特性**:
- ✅ 文件名、大小、类型
- ✅ 上传进度条（模拟）
- ✅ 删除按钮（重新选择）

### 2. 上传按钮

**之前**:
```
[ 开始处理 ]
```

**现在**:
```
[ 📤 上传文档 ]

文档将以"待处理"状态保存，稍后可进行处理和向量化
```

### 3. 成功消息

**之前**:
```
✅ 文档上传成功！正在处理中...
```

**现在**:
```
✅ 文档上传成功！文件已保存至知识库，状态为"待处理"。
   即将跳转到仪表板...
```

### 4. Dashboard 状态显示

| 状态 | 图标 | 颜色 | 说明 |
|------|------|------|------|
| **pending** | 🔵 | 蓝色 | 待处理（已上传，未处理） |
| **processing** | ⏳ | 黄色 | 处理中（正在 chunk/向量化） |
| **ready** | ✅ | 绿色 | 就绪（可用于 RAG） |
| **failed** | ❌ | 红色 | 失败（处理出错） |

---

## 🔧 技术实现

### 修改的文件

#### 1. `app/admin/upload/page.tsx`

**新增功能**:
```typescript
// 文件预览状态
const [filePreview, setFilePreview] = useState<{
  name: string
  size: string
  type: string
} | null>(null)

// 上传进度
const [uploadProgress, setUploadProgress] = useState(0)

// 文件大小格式化
const formatFileSize = (bytes: number): string => {
  // 将字节转换为 KB/MB/GB
}

// 选择文件时显示预览
const handleFileChange = (e) => {
  const file = e.target.files[0]
  setFilePreview({
    name: file.name,
    size: formatFileSize(file.size),
    type: file.type,
  })
}

// 上传时显示进度
const handleSubmit = async (e) => {
  // 模拟进度条
  const progressInterval = setInterval(() => {
    setUploadProgress(prev => prev + 10)
  }, 200)
  
  // 上传完成后
  setUploadProgress(100)
}
```

**新增组件**:
- ✅ 文件预览卡片
- ✅ 上传进度条
- ✅ 删除文件按钮
- ✅ 详细的控制台日志

#### 2. `app/api/documents/route.ts`

**关键修改**:
```typescript
// 修改前
const { data: document, error: dbError } = await supabase
  .from('documents')
  .insert({
    // ...
    status: 'processing', // ❌ 旧状态
  })

// 修改后
const { data: document, error: dbError } = await supabase
  .from('documents')
  .insert({
    // ...
    status: 'pending', // ✅ 新状态
  })
```

#### 3. `app/admin/dashboard/page.tsx`

**新增状态显示**:
```typescript
{doc.status === "ready" ? (
  <><CheckCircle2 /><span>就绪</span></>
) : doc.status === "processing" ? (
  <><Clock className="animate-spin" /><span>处理中</span></>
) : doc.status === "pending" ? (
  <><HelpCircle /><span>待处理</span></> // ✅ 新增
) : doc.status === "failed" ? (
  <><AlertCircle /><span>失败</span></>
) : (
  <><HelpCircle /><span>未知</span></>
)}
```

---

## 🧪 测试步骤

### 测试 1: 文件预览

1. 访问 `/admin/upload`
2. **拖放**一个 PDF 文件到上传区域
3. ✅ 应该看到文件预览卡片（名称、大小、类型）
4. 点击删除按钮（垃圾桶图标）
5. ✅ 文件预览消失，可以重新选择

### 测试 2: 上传进度

1. 选择文件并填写表单
2. 点击"上传文档"按钮
3. ✅ 按钮显示"上传中... X%"
4. ✅ 文件预览卡片显示进度条
5. ✅ 进度从 0% → 100%
6. ✅ 显示绿色成功消息
7. ✅ 3秒后自动跳转到 Dashboard

### 测试 3: 控制台日志

打开浏览器 DevTools Console，上传文件时应该看到：

```
📤 开始上传文件: {
  fileName: "merchant-guide.pdf",
  fileSize: "2.3 MB",
  title: "商户指南",
  category: "商品管理"
}

✅ 上传成功: {
  message: "文档上传成功",
  document: {
    id: "uuid",
    status: "pending", // ← 确认是 "pending"
    ...
  }
}
```

### 测试 4: Dashboard 状态

1. 访问 `/admin/dashboard`
2. ✅ 刚上传的文档应该显示
3. ✅ 状态列显示 🔵 **待处理**（蓝色）
4. ✅ 不是 ⏳ "处理中"（黄色）

### 测试 5: 数据库验证

在 Supabase SQL Editor 中运行：

```sql
SELECT 
  id,
  title,
  status,
  file_path,
  created_at
FROM documents
ORDER BY created_at DESC
LIMIT 1;
```

**预期结果**:
```
status: "pending"  ← 确认是 "pending"
```

---

## 📊 状态转换流程

### 完整的文档生命周期

```
上传
  ↓
pending (待处理)
  ↓ [手动触发或自动任务]
processing (处理中)
  ↓ [PDF解析 + Chunk + 向量化]
ready (就绪) ← 可用于 RAG
  ↓
  或
  ↓
failed (失败) ← 处理出错
```

### 各状态的含义

| 状态 | 含义 | 文件在 Storage | DB 记录 | Chunks | 可用于 RAG |
|------|------|---------------|---------|--------|-----------|
| **pending** | 已上传，待处理 | ✅ | ✅ | ❌ | ❌ |
| **processing** | 正在处理 | ✅ | ✅ | 🔄 | ❌ |
| **ready** | 处理完成 | ✅ | ✅ | ✅ | ✅ |
| **failed** | 处理失败 | ✅ | ✅ | ❌ | ❌ |

---

## 🚀 Phase 4 准备

### 需要实现的功能

#### 1. 处理 API (手动触发方案)

创建新的 API 端点：

```typescript
// app/api/documents/[id]/process/route.ts

export async function POST(request, { params }) {
  const { id } = params
  
  // 1. 获取文档记录
  const document = await getDocument(id)
  
  // 2. 更新状态为 processing
  await updateDocumentStatus(id, 'processing')
  
  // 3. 下载文件
  const fileContent = await downloadFromStorage(document.file_path)
  
  // 4. 解析内容（PDF/TXT/MD）
  const text = await parseDocument(fileContent, document.content_type)
  
  // 5. 切分文本
  const chunks = await chunkText(text, {
    chunkSize: 1000,
    chunkOverlap: 200
  })
  
  // 6. 生成向量
  for (const chunk of chunks) {
    const embedding = await generateEmbedding(chunk.content)
    await insertChunk({
      document_id: id,
      content: chunk.content,
      embedding: embedding
    })
  }
  
  // 7. 更新状态为 ready
  await updateDocumentStatus(id, 'ready')
  
  return NextResponse.json({ success: true })
}
```

#### 2. Dashboard "开始处理"按钮

在 Dashboard 的每一行添加"开始处理"按钮（仅对 pending 状态显示）：

```typescript
{doc.status === 'pending' && (
  <Button
    size="sm"
    onClick={() => handleProcess(doc.id)}
    disabled={processingId === doc.id}
  >
    {processingId === doc.id ? (
      <>
        <Clock className="animate-spin" />
        处理中...
      </>
    ) : (
      <>
        <Play />
        开始处理
      </>
    )}
  </Button>
)}
```

#### 3. 后台任务（自动方案）

创建 Next.js API Route 作为 Cron Job：

```typescript
// app/api/cron/process-pending/route.ts

export async function GET(request) {
  // 验证 cron secret
  if (request.headers.get('Authorization') !== `Bearer ${process.env.CRON_SECRET}`) {
    return new Response('Unauthorized', { status: 401 })
  }
  
  // 查询所有 pending 文档
  const { data: pendingDocs } = await supabase
    .from('documents')
    .select('*')
    .eq('status', 'pending')
    .limit(10) // 每次处理 10 个
  
  // 逐个处理
  for (const doc of pendingDocs) {
    await fetch(`/api/documents/${doc.id}/process`, {
      method: 'POST'
    })
  }
  
  return NextResponse.json({ processed: pendingDocs.length })
}
```

配置 Vercel Cron（`vercel.json`）:
```json
{
  "crons": [
    {
      "path": "/api/cron/process-pending",
      "schedule": "*/5 * * * *"
    }
  ]
}
```

---

## ✅ 验证清单

- [x] 文件拖放后显示预览
- [x] 上传时显示进度条
- [x] 上传成功显示绿色消息
- [x] 自动跳转到 Dashboard
- [x] Dashboard 显示"待处理"状态（蓝色）
- [x] 数据库 status 字段为 'pending'
- [x] 控制台有详细日志
- [x] 支持删除已选文件并重新选择
- [ ] 实现"开始处理"功能（Phase 4）
- [ ] 实现 PDF 解析（Phase 4）
- [ ] 实现文本切分（Phase 4）
- [ ] 实现向量化（Phase 4）

---

## 🎯 总结

### ✅ 已完成（Phase 3）

- [x] 文件上传到 Storage
- [x] 创建数据库记录（status='pending'）
- [x] 文件预览和上传进度
- [x] Dashboard 显示多种状态

### ⏳ 待实现（Phase 4）

- [ ] PDF/TXT/MD 解析
- [ ] 文本切分（Chunking）
- [ ] 向量化（Embeddings）
- [ ] "开始处理"按钮或后台任务

---

**工作流程更新完成！** 🎉

现在文档上传后会以"待处理"状态保存，等待 Phase 4 实现处理功能。

